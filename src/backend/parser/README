src/backend/parser/README

解析器（Parser）
================

本目录所做的不仅仅是把 SQL 查询进行词法分析和语法分析。它还会为传递给优化器、进而传递给执行器的各种复杂查询创建 Query 结构。

parser.c        此处开始处理
scan.l          将查询拆分为词法单元（token）
scansup.c       处理输入字符串中的转义字符
gram.y          解析词法单元并生成一棵“原始”解析树
analyze.c       可优化查询的解析分析顶层入口
parse_agg.c     处理聚集函数，如 SUM(col1)、AVG(col2)、...
parse_clause.c  处理 WHERE、ORDER BY、GROUP BY 等子句
parse_coerce.c  处理将表达式强制转换为不同数据类型
parse_collate.c 为已完成的表达式分配排序规则（collation）信息
parse_cte.c     处理公用表表达式（WITH 子句）
parse_expr.c    处理 col、col + 3、x = 3 或 x = 4 这类表达式
parse_enr.c     处理临时命名关系（如触发器过渡表等）
parse_func.c    处理函数、table.column 以及列标识符
parse_jsontable.c 处理 JSON_TABLE
parse_merge.c   处理 MERGE
parse_node.c    为各种结构创建节点
parse_oper.c    处理表达式中的操作符
parse_param.c   处理参数 Param（针对核心后端中使用的那些情形）
parse_relation.c 处理表与列的辅助例程
parse_target.c  处理查询的结果列表
parse_type.c    处理数据类型的辅助例程
parse_utilcmd.c 对实用命令（utility command）进行解析分析（在执行时完成）

另请参见 src/common/keywords.c，其中包含了标准关键字的表以及关键字查找函数。我们将它单独拆分出来，是因为各种前端代码也希望使用它。
