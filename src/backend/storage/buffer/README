src/backend/storage/buffer/README

共享缓冲区访问规则说明
======================

共享磁盘缓冲区有两套独立的访问控制机制：
引用计数（又称 pin 计数）和缓冲区内容锁。（实际上，
还有第三层访问控制：在合法访问属于某个关系的任何页之前，
必须先持有该关系上适当类型的锁。关系级锁不在本文讨论范围内。）

Pin：在对缓冲区进行任何操作之前，必须先"持有该缓冲区的 pin"
（递增其引用计数）。未 pin 的缓冲区随时可能被回收并重新用于
另一个页面，因此触碰它是不安全的。通常通过 ReadBuffer 获取 pin，
通过 ReleaseBuffer 释放 pin。单个后端并发地多次 pin 同一个页面
是允许的，实际上也很常见；缓冲区管理器能高效地处理这种情况。
长时间持有 pin 也是允许的——例如，顺序扫描会对当前页面持有 pin，
直到该页上所有元组都处理完毕，如果该扫描是连接的驱动表扫描，
这可能需要相当长的时间。同样，B树索引扫描也可能对当前索引页
持有 pin。这是允许的，因为普通操作从不等待页面的 pin 计数降为零。
（任何可能需要做这种等待的操作，改为通过等待获取关系级锁来处理，
这就是为什么你最好先持有一个关系级锁。）但是，pin 不能跨越事务边界持有。

缓冲区内容锁：有两种缓冲区锁，共享锁和排他锁，
其行为正如你所预期：多个后端可以同时持有同一缓冲区的共享锁，
但排他锁会阻止其他任何人持有共享锁或排他锁。
（这些也可称为 READ 锁和 WRITE 锁。）这些锁旨在短期持有：
不应长时间持有。缓冲区锁通过 LockBuffer() 获取和释放。
单个后端试图在同一缓冲区上获取多个锁是*不会*成功的。
在尝试锁定缓冲区之前，必须先 pin 住它。

缓冲区访问规则：

1. 要扫描页面中的元组，必须持有 pin 以及共享或排他内容锁。
要检查共享缓冲区中元组的提交状态（XID 和状态位），
同样必须持有 pin 以及共享或排他锁。

2. 一旦确定某个元组是有意义的（对当前事务可见），
就可以释放内容锁，但只要还持有缓冲区 pin，
就可以继续访问该元组的数据。这是堆扫描的典型做法，
因为 heap_fetch 返回的元组包含指向共享缓冲区中元组数据的指针。
因此，在持有 pin 期间，该元组不会消失（参见规则 #5）。
它的状态可能会改变，但可以假定在初始可见性判断之后，
这些改变不重要。

3. 要添加元组或修改现有元组的 xmin/xmax 字段，
必须对包含该元组的缓冲区持有 pin 和排他内容锁。
这确保其他任何人在进行可见性检查时不会看到元组的部分更新状态。

4. 仅持有缓冲区的共享锁和 pin 时，更新元组提交状态位是可以的
（即，将 HEAP_XMIN_COMMITTED、HEAP_XMIN_INVALID、HEAP_XMAX_COMMITTED
或 HEAP_XMAX_INVALID 值 OR 到 t_infomask 中）。
这是可以的，因为另一个后端大约在同一时间查看该元组时，
也会将相同的位 OR 到该字段中，因此冲突更新的风险很小甚至没有；
再者，即使确实发生冲突，也不过是某次位更新丢失，稍后需要重新执行而已。
这四个位仅仅是提示（它们缓存了 pg_xact 中事务状态查找的结果），
因此即使被冲突更新重置为零，也不会造成重大损害。
但请注意，元组的冻结是通过同时设置 HEAP_XMIN_INVALID
和 HEAP_XMIN_COMMITTED 来完成的；这是一个关键更新，
因此需要排他缓冲区锁（并且还必须记录 WAL）。

5. 要物理删除元组或压缩页面上的空闲空间，
必须持有 pin 和排他锁，*并且*在持有排他锁期间观察到
缓冲区的共享引用计数为 1（即，没有其他后端持有 pin）。
如果满足这些条件，则其他后端在排他锁释放之前都无法进行页面扫描，
也不会有其他后端持有任何现有元组的引用以期望再次检查它。
注意，其他后端可能在此清理操作进行期间 pin 住该缓冲区
（递增引用计数），但它在获取共享或排他内容锁之前无法实际检查该页面。


获取规则 #5 所需的锁由 bufmgr 例程
LockBufferForCleanup() 或 ConditionalLockBufferForCleanup() 完成。
它们首先获取排他锁，然后检查当前共享 pin 计数是否为 1。
如果不是，则 ConditionalLockBufferForCleanup() 释放排他锁并返回 false，
而 LockBufferForCleanup() 释放排他锁（但不释放调用方的 pin），
并等待直到被其他后端发信号通知，之后它会重试。
信号在 UnpinBuffer 将共享 pin 计数递减到 1 时发出。
如上所述，这个操作在获取锁之前可能需要等待相当长的时间，
但对于并发 VACUUM 来说应该影响不大。
当前实现仅支持在任何特定共享缓冲区上有一个等待 pin-count-1 的等待者。
这对于 VACUUM 的使用已经足够，因为我们无论如何也不允许多个 VACUUM
在一个关系上并发运行。任何希望在恢复或 VACUUM 之外获取清理锁的人，
必须使用条件变体版本的函数。


缓冲区管理器的内部锁机制
-------------------------

在 PostgreSQL 8.1 之前，共享缓冲区管理器本身的所有操作
都由一个系统级锁 BufMgrLock 保护，不出所料，这成为了竞争的热点。
新的锁方案避免了在常见代码路径中获取系统级排他锁。其工作原理如下：

* 存在一个系统级 LWLock，即 BufMappingLock，它在概念上
保护从缓冲区标签（页面标识符）到缓冲区的映射。
（物理上，可以认为它保护了 buf_table.c 维护的哈希表。）
要查找某个标签是否存在对应的缓冲区，获取 BufMappingLock 的共享锁即可。
请注意，在释放 BufMappingLock 之前，必须 pin 住找到的缓冲区（如果有的话）。
要更改任何缓冲区的页面分配，必须持有 BufMappingLock 的排他锁。
此锁必须在调整缓冲区头部字段和更改 buf_table 哈希表的整个过程中一直持有。
唯一需要排他锁的常见操作是读入一个尚未在共享缓冲区中的页面，
这至少需要一个内核调用，通常还需要等待 I/O，因此无论如何都会很慢。

* 从 PG 8.2 开始，BufMappingLock 已被拆分为 NUM_BUFFER_PARTITIONS 个
独立的锁，每个锁守护缓冲区标签空间的一部分。这进一步减少了
正常代码路径中的竞争。特定缓冲区标签所属的分区由
标签哈希值的低位决定。上述规则分别适用于每个分区。
如果需要同时锁定多个分区，必须按分区号顺序锁定，以避免死锁风险。

* 一个单独的系统级自旋锁 buffer_strategy_lock，
为访问缓冲区空闲列表或选择缓冲区进行替换的操作提供互斥。
此处使用自旋锁而非轻量级锁是为了效率；
在持有 buffer_strategy_lock 期间，不应获取任何其他类型的锁。
这对于允许多个后端以合理的并发度进行缓冲区替换至关重要。

* 每个缓冲区头部包含一个自旋锁，在检查或更改该缓冲区头部的字段时
必须持有该锁。这使得 ReleaseBuffer 等操作可以在不获取任何系统级锁的
情况下进行本地状态更改。我们使用自旋锁而非 LWLock，
因为不存在需要持有锁超过几条指令长的情况。

请注意，缓冲区头部的自旋锁不控制对缓冲区中数据的访问。
每个缓冲区头部还包含一个 LWLock，即"缓冲区内容锁"，
它*确实*代表访问缓冲区中数据的权限。按照上述规则使用它。

* BM_IO_IN_PROGRESS 标志充当一种锁，用于等待缓冲区上的 I/O 完成
（在 14 之前的版本中，它还伴随一个 per-buffer 的 LWLock）。
发起读取或写入的进程设置该标志。当 I/O 完成时——
无论是发起 I/O 的进程还是另一个进程完成——该标志被清除，
并且缓冲区的条件变量被发信号通知。
需要等待 I/O 完成的进程可以通过使用 BufferDesc->io_wref
以及通过睡眠在缓冲区条件变量上来等待 BM_IO_IN_PROGRESS 被清除。异步 I/O 也可以通过这种方式等待。


常规缓冲区替换策略
-------------------

存在一个作为主要替换候选的缓冲区"空闲列表"。
特别地，完全空闲（不包含有效页面）的缓冲区始终在该列表中。
如果我们认为某个页面的内容不太可能很快被需要，
也可以将其投入此列表；不过，当前算法从未这样做。
该列表通过缓冲区头部中的字段构成单向链表；
我们在全局变量中维护头指针和尾指针。
（注意：虽然链表链接存储在缓冲区头部中，
但它们被视为受 buffer_strategy_lock 保护，而非缓冲区头部自旋锁保护。）
当没有可用空闲缓冲区时，为了选择一个牺牲缓冲区进行回收，
我们使用简单的时钟扫描算法，这避免了在常见操作期间需要获取系统级锁。
其工作原理如下：

每个缓冲区头部包含一个使用计数器，每当缓冲区被 pin 时
递增（最高递增到一个小限制值）。（这仅需要缓冲区头部自旋锁，
而无论如何都需要获取该锁来递增缓冲区引用计数，所以几乎没有额外开销。）

"时钟指针"是一个缓冲区索引 nextVictimBuffer，
在所有可用缓冲区中循环移动。nextVictimBuffer 受 buffer_strategy_lock 保护。

需要获取牺牲缓冲区的进程算法如下：

1. 获取 buffer_strategy_lock。

2. 如果缓冲区空闲列表非空，取出其头部缓冲区。
释放 buffer_strategy_lock。
如果该缓冲区被 pin 或具有非零使用计数，则不能使用；
忽略它，回到步骤 1。否则，pin 住该缓冲区并返回它。

3. 否则，缓冲区空闲列表为空。选择 nextVictimBuffer 指向的缓冲区，
并循环推进 nextVictimBuffer 以备下次使用。
释放 buffer_strategy_lock。

4. 如果选中的缓冲区被 pin 或具有非零使用计数，则不能使用。
递减其使用计数（如果非零），重新获取 buffer_strategy_lock，
返回步骤 3 检查下一个缓冲区。

5. Pin 住选中的缓冲区，并返回。

（注意：如果选中的缓冲区是脏的，在回收之前我们必须将其写出；
如果在此期间其他人 pin 住了该缓冲区，我们必须放弃并尝试另一个缓冲区。
但这不属于基本的牺牲缓冲区选择算法的关注范围。）


缓冲区环形替换策略
-------------------

当运行只需要访问大量页面一次的查询时，
如 VACUUM 或大型顺序扫描，会使用不同的策略。
仅由此类扫描触碰过的页面不太可能很快再次被需要，
因此不运行常规的时钟扫描算法来冲刷整个缓冲区缓存，
而是使用常规时钟扫描算法分配一个小型环形缓冲区，
并在整个扫描过程中重用这些缓冲区。
这也意味着此类语句产生的大量写流量将由后端自身完成，
而不会推给其他进程。

对于顺序扫描，使用 256KB 的环形缓冲区。这个大小足够小以放入 L2 缓存，
使得从 OS 缓存传输页面到共享缓冲区缓存更加高效。
更小的值通常也足够，但环形缓冲区必须足够大以容纳扫描中所有
并发 pin 住的页面。256KB 也应足够为其他后端留下一个小缓存轨迹，
以便加入同步顺序扫描。如果环形缓冲区被弄脏并且其 LSN 更新了，
我们通常必须在重用该缓冲区之前写入并刷新 WAL；
在这种情况下，我们改为从环形缓冲区中丢弃该缓冲区，
并（稍后）使用常规时钟扫描算法选择替换。
因此，此策略最适合只读扫描（或最多只更新提示位的扫描）。
在修改扫描中每个页面的扫描中，如批量 UPDATE 或 DELETE，
环形缓冲区中的页面将始终被弄脏，环形策略实际上退化为常规策略。

VACUUM 使用与顺序扫描类似的环形缓冲区，但此环形缓冲区的大小
由 vacuum_buffer_usage_limit GUC 控制。脏页不会从环形缓冲区中移除。
相反，如果需要，会刷新 WAL 以允许重用缓冲区。
在 8.3 中引入缓冲区环形策略之前，VACUUM 的缓冲区被发送到空闲列表，
这实际上是一个单缓冲区的环形缓冲区，导致 WAL 刷新过度。

批量写入的工作方式与 VACUUM 类似。目前这仅适用于
COPY IN 和 CREATE TABLE AS SELECT。
（是否值得让顺序扫描的 UPDATE 和 DELETE 也使用批量写入策略？）
对于批量写入，我们使用 16MB 的环形缓冲区大小
（但不超过 shared_buffers 的 1/8）。
更小的尺寸已被证明会导致 COPY 因 WAL 刷新而阻塞过于频繁。
虽然后台 VACUUM 因自身执行 WAL 刷新而变慢是可行的，
但我们希望 COPY 不受此影响，因此让它使用稍多一些的缓冲区空间。


后台写入器处理
---------------

后台写入器旨在写出可能很快会被回收的页面，
从而将写入工作从活跃后端卸载下来。
为此，它从 nextVictimBuffer 的当前位置开始循环向前扫描
（它不会改变 nextVictimBuffer！），
寻找脏的、未被 pin 且未标记正使用计数的缓冲区。
它对任何此类缓冲区进行 pin、写入和释放。

如果我们可以假设读取 nextVictimBuffer 是原子操作，
那么写入器甚至不需要获取 buffer_strategy_lock 来寻找要写入的缓冲区；
它只需要自旋锁定每个缓冲区头部足够长的时间来检查脏位即可。
即使没有这个假设，写入器也只需要获取锁足够长的时间来读取变量值，
而不是在扫描缓冲区期间一直持锁。
（与 PG 8.0 相比，这在写入器的竞争成本方面是一个非常大的改进。）

后台写入器在写出缓冲区时对其持有共享内容锁
（其他任何将缓冲区内容刷新到磁盘的人也必须这样做）。
这确保了传输到磁盘的页面映像在一定程度上是一致的。
我们可能会错过一两个提示位更新，但这不成问题，
原因与缓冲区访问规则中提到的相同。

从 8.4 开始，当存在某种可能较长时间的恢复需要执行时，
后台写入器在恢复模式下启动。它执行与正常处理相同的服务，
只是它写入的检查点在技术上称为重启点（restartpoint）。
