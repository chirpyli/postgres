src/backend/storage/smgr/README

存储管理器
================

在最初的 Berkeley Postgres 系统中，存在多个存储管理器，其中仅保留了
“磁盘”（magnetic disk）管理器。（在 Berkeley 还曾有用于 Sony WORM 光盘
自动换盘机和持久主存的存储管理器，但它们从未在 Berkeley 之外发布的任何
Postgres 版本，也未在任何 PostgreSQL 版本中得到支持。）“磁盘”管理器这个
名字其实名不副实，因为它实际上支持任何操作系统提供标准文件系统操作的
设备；而如今这类设备几乎涵盖了所有我们关心的存储。不过，我们仍然保留了
存储管理器切换层的概念，以防将来有人希望重新引入其他类型的存储管理器。
移除这一切换层并不会带来任何明显的收益，因为存储访问操作的开销显然远远
大于多一层 C 函数调用。

在 Berkeley Postgres 中，每个关系都被打上了要使用哪个存储管理器的 ID
标签。这一点已经不复存在。如果将来要把多个存储管理器重新引入系统目录，
更合理的做法是将存储管理器与表空间（tablespace）关联起来。

本目录中的文件及其内容如下：

    smgr.c	存储管理器切换的派发代码。本文件中的例程会调用相应的
		存储管理器来执行上层代码所请求的存储访问。smgr.c 还管理
		文件句柄缓存（SMgrRelation 表）。

    md.c	“磁盘”存储管理器，实际上只是对内核文件系统操作的一层
		接口。

注意，md.c 转而依赖于 src/backend/storage/file/fd.c。


关系的 Fork
==============

自 8.4 起，一个 smgr 关系可以由多个物理文件组成，这些文件称为关系的
fork。这使得额外的元数据（如空闲空间信息）可以存放在额外的 fork 中，
这些 fork 能够独立于主数据文件进行扩展和截断，同时在系统目录中仍被
视为一个单一的物理关系。

假定主 fork（fork 编号 0，即 MAIN_FORKNUM）始终存在。fork 编号在
src/include/common/relpath.h 中分配。smgr.c 和 md.c 中的函数除了
relfilelocator 和块号之外，还接受一个额外的 fork 编号参数，用以标识
你想要访问的是哪个关系 fork。由于大多数代码都想访问主 fork，缓冲区管理
器中为了方便提供了一个访问 MAIN_FORKNUM 的 ReadBuffer 快捷版本。
